extends layout

block content

 div(style="font-size: 14px")
   
  #wrapper.d-flex

   #sidebar-wrapper.bg-light.border-right
    .sidebar-heading 
    .list-group.list-group-flush
      a.list-group-item.list-group-item-action.bg-light(href='/posts') Posts
      a.list-group-item.list-group-item-action.bg-light(href='/recursos') Recursos
      a.list-group-item.list-group-item-action.bg-light(href='/perfil') Perfil

   #page-content-wrapper
    nav.navbar.navbar-expand-lg.navbar-light.bg-light
      button#menu-toggle.material-icons(style="position: absolute") list

    .container-fluid
      div(style="width: 70%; margin: auto")

        p(style="margin-left: 2%; font-size:22px; font-weight: bold") Noticias 
 
        table(class="w3-table-all")
         each r in noticias
            tr
              if (r.tipo=="post")
                td= "Foi adicionado um novo post por " + r.autor + "!"
                td= r.data
                td
                  a(href='/post/' + r._id) Ver Mais
              else 
                td= "Foi adicionado um novo recurso por " + r.produtor + "!"
                td= r.dataCriacao
                td
                  a(href='/recursos/' + r._id) Ver Mais
 


 script(src='vendor/jquery/jquery.min.js')
 script(src='vendor/bootstrap/js/bootstrap.bundle.min.js')

 script.
   $("#menu-toggle").click(function(e) {
   e.preventDefault();
   $("#wrapper").toggleClass("toggled");
   });